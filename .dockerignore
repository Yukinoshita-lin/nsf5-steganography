# 构建上下文按仓库根算, 而根目录下有 data/(2.5GB) gpu/(2.6GB) .git/ 等
# 大体积内容。不过滤的话每次 docker build 都要把它们发给 daemon。
# 镜像只需要 pyproject/README + src/notebooks/scripts/teaching + models/。

# 版本控制与 IDE
.git
.gitignore
.github
.idea
.vscode
.DS_Store

# Python 缓存与构建产物
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.venv/
venv/

# 大体积本地数据集与生成物 (镜像里用不到, 教学镜像自带 demo 数据)
data/
gpu/
thesis/
output/
img/
docs/
_pdf_build/
_tpl/
teaching/videos/
teaching/web/*/_build/
notebooks/.ipynb_checkpoints/
webapp/tests/node_modules/
yccstego/

# 模型: 逐条排除不需要的备份/废弃产物, **不用否定规则**。
# 曾经写成 `models/*.joblib` + `!models/stego_classifier.joblib` 这种白名单,
# 结果那个例外没有生效 (Docker 的 .dockerignore 否定语义与 .gitignore 并不完全
# 一致), 两个部署模型被挡在构建上下文外, 镜像构建在 test_pipeline.py 的
# "模型未能加载" 断言上失败。教训: 排除模式绝不应该有可能命中必需文件 ——
# 用黑名单逐条排, 漏排最多是上下文大一点, 永远不会是功能不可用。
models/*.pt
models/*_weak*.joblib
models/*_gpu*.joblib
models/*_campus_srm.joblib
models/*_tuned*.joblib
models/*.bak.joblib
models/stego_classifier_v2_jpeg.joblib
models/stego_classifier_v2_jpeg_olddefault*
