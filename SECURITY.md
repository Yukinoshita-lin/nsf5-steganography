# 安全政策

## 这个项目的性质

`nsf5stego` 是**教学与研究**工具：它演示经典隐写与隐写分析算法，用来上课、
做实验、复现论文。它**不是**为真实隐蔽通信或数据保护设计的，请不要当作安全产品使用。

已知边界（也写在手册里，避免误解）：

- 哈希键控提供的是**篡改感知**，不是密码学完整性：它没有用密钥做 MAC，
  有能力整图重嵌的攻击者可以同步更新头部哈希。
- 嵌入是可以被检测的：`src/steganalysis.py` 与 ML 模型的输出是**概率**，
  不能作为任何形式的证据。
- 在受监管场景使用隐写技术可能违法；合规责任在使用者。

## 报告漏洞

如果发现**代码层面的**问题（例如解析恶意图像时崩溃、整数溢出、路径穿越、
依赖供应链问题），请优先用 GitHub 的
[私有漏洞报告](https://github.com/Yukinoshita-lin/nsf5-steganography/security/advisories/new)；
不方便时也可以直接开 issue（请不要在里面粘贴可利用的细节）。

我们关心的问题类型：

- 解析不受信任输入时的崩溃 / 越界（`src/ns5_core.py`、`src/image_io.py`、
  `src/make_dataset.py`、`yccstego` 的 JPEG 解析路径）；
- C++ 加速库（`cpp/*.cpp`）的内存安全问题；
- 依赖或 CI 配置里可能被利用的地方（例如 workflow 的权限、缓存投毒）。

## 不处理的问题

- **算法可检测性**：隐写被卡方/RS/ML 检出，是算法的性质，不是漏洞。
- **模型准确率**：误报/漏报属于研究方法问题，请开普通 issue，并附上
  语料与协议（见 `docs/RESULTS.md` 的口径说明）。
- **第三方数据集**：BOSSbase、ALASKA#2、DIV2K 的获取与许可请找各自的发布方；
  本仓库不重新分发它们。

## 响应方式

这是个人维护的教学项目，没有 SLA。我们会在力所能及的范围内确认、修复，
并在 `CHANGELOG.md` 里如实写明影响范围与修复方式。
